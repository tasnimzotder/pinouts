import { PinFuncType } from '@interfaces/pinFunc.interface';

const Power: PinFuncType = {
  id: 'power',
  name: 'Power',
  description: 'Power',
  used_for: ['Powering the ESP32'],
};

export default Power;
