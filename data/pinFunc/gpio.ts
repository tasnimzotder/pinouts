import { PinFuncType } from '@interfaces/pinFunc.interface';

const GPIO: PinFuncType = {
  id: 'gpio',
  name: 'GPIO',
  description: 'General Purpose Input Output',
  used_for: ['Connecting to external devices'],
};

export default GPIO;
